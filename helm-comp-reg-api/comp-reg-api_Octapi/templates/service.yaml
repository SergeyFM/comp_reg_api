apiVersion: v1
kind: Service
metadata:
  name: {{ include "comp-reg-api.fullname" . }}
  labels:
    {{- include "comp-reg-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "comp-reg-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
